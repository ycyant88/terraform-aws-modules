[
  {
    "name": "task_iam_role_name",
    "type": "string",
    "description": "Name to use on IAM role created",
    "default": "null",
    "required": false
  },
  {
    "name": "region",
    "type": "string",
    "description": "Region where the resource(s) will be managed. Defaults to the Region set in the provider configuration",
    "default": "null",
    "required": false
  },
  {
    "name": "create_execution_iam_role",
    "type": "bool",
    "description": "Determines whether the ECS task definition IAM role should be created",
    "default": "true",
    "required": false
  },
  {
    "name": "execution_iam_role_path",
    "type": "string",
    "description": "IAM role path",
    "default": "null",
    "required": false
  },
  {
    "name": "memory",
    "type": "string",
    "description": "Amount of memory (in MiB) used by the task. Valid values are between `512` and `8192`",
    "default": "null",
    "required": false
  },
  {
    "name": "scaling_target",
    "type": "object({\n    auto_scaling_metric       = string\n    auto_scaling_target_value = number\n    max_task_count            = number\n    min_task_count            = number\n  })",
    "description": "The auto-scaling configuration for this service revision",
    "default": "null",
    "required": false
  },
  {
    "name": "security_group_name",
    "type": "string",
    "description": "Name to use on security group created",
    "default": "null",
    "required": false
  },
  {
    "name": "cloudwatch_log_group_name",
    "type": "string",
    "description": "Custom name of CloudWatch Log Group for ECS cluster",
    "default": "null",
    "required": false
  },
  {
    "name": "create",
    "type": "bool",
    "description": "Determines whether resources will be created (affects all resources)",
    "default": "true",
    "required": false
  },
  {
    "name": "execution_ssm_param_arns",
    "type": "list(string)",
    "description": "List of SSM parameter ARNs the task execution role will be permitted to get/read",
    "default": "[]",
    "required": false
  },
  {
    "name": "task_iam_role_statements",
    "type": "map(object({\n    sid           = optional(string)\n    actions       = optional(list(string))\n    not_actions   = optional(list(string))\n    effect        = optional(string)\n    resources     = optional(list(string))\n    not_resources = optional(list(string))\n    principals = optional(list(object({\n      type        = string\n      identifiers = list(string)\n    })))\n    not_principals = optional(list(object({\n      type        = string\n      identifiers = list(string)\n    })))\n    condition = optional(list(object({\n      test     = string\n      values   = list(string)\n      variable = string\n    })))\n  }))",
    "description": "A map of IAM policy [statements](https://registry.terraform.io/providers/hashicorp/aws/latest/docs/data-sources/iam_policy_document#statement) for custom permission usage",
    "default": "null",
    "required": false
  },
  {
    "name": "primary_container",
    "type": "object({\n    aws_logs_configuration = optional(object({\n      log_group         = string\n      log_stream_prefix = string\n    }))\n    command        = optional(list(string))\n    container_port = optional(number)\n    environment = optional(list(object({\n      name  = string\n      value = string\n    })))\n    image = string\n    repository_credentials = optional(object({\n      credentials_parameter = string\n    }))\n    secret = optional(list(object({\n      name       = string\n      value_from = string\n    })))\n  })",
    "description": "The primary container configuration for this service revision",
    "default": "null",
    "required": false
  },
  {
    "name": "execution_iam_role_name",
    "type": "string",
    "description": "Name to use on IAM role created",
    "default": "null",
    "required": false
  },
  {
    "name": "task_iam_role_description",
    "type": "string",
    "description": "Description of the role",
    "default": "null",
    "required": false
  },
  {
    "name": "create_execution_policy",
    "type": "bool",
    "description": "Determines whether the ECS task definition IAM policy should be created. This includes permissions included in AmazonECSTaskExecutionRolePolicy as well as access to secrets and SSM parameters",
    "default": "true",
    "required": false
  },
  {
    "name": "infrastructure_iam_role_use_name_prefix",
    "type": "bool",
    "description": "Determines whether the IAM role name (`iam_role_name`) is used as a prefix",
    "default": "true",
    "required": false
  },
  {
    "name": "infrastructure_iam_role_tags",
    "type": "map(string)",
    "description": "A map of additional tags to add to the IAM role created",
    "default": "{}",
    "required": false
  },
  {
    "name": "cloudwatch_log_group_kms_key_id",
    "type": "string",
    "description": "If a KMS Key ARN is set, this key will be used to encrypt the corresponding log group. Please be sure that the KMS Key has an appropriate key policy (https://docs.aws.amazon.com/AmazonCloudWatch/latest/logs/encrypt-log-data-kms.html)",
    "default": "null",
    "required": false
  },
  {
    "name": "security_group_description",
    "type": "string",
    "description": "Description of the security group created",
    "default": "null",
    "required": false
  },
  {
    "name": "execution_iam_role_tags",
    "type": "map(string)",
    "description": "A map of additional tags to add to the IAM role created",
    "default": "{}",
    "required": false
  },
  {
    "name": "execution_iam_statements",
    "type": "map(object({\n    sid           = optional(string)\n    actions       = optional(list(string))\n    not_actions   = optional(list(string))\n    effect        = optional(string)\n    resources     = optional(list(string))\n    not_resources = optional(list(string))\n    principals = optional(list(object({\n      type        = string\n      identifiers = list(string)\n    })))\n    not_principals = optional(list(object({\n      type        = string\n      identifiers = list(string)\n    })))\n    condition = optional(list(object({\n      test     = string\n      values   = list(string)\n      variable = string\n    })))\n  }))",
    "description": "A map of IAM policy [statements](https://registry.terraform.io/providers/hashicorp/aws/latest/docs/data-sources/iam_policy_document#statement) for custom permission usage",
    "default": "null",
    "required": false
  },
  {
    "name": "task_iam_role_arn",
    "type": "string",
    "description": "Existing IAM role ARN",
    "default": "null",
    "required": false
  },
  {
    "name": "task_iam_role_permissions_boundary",
    "type": "string",
    "description": "ARN of the policy that is used to set the permissions boundary for the IAM role",
    "default": "null",
    "required": false
  },
  {
    "name": "task_iam_role_policies",
    "type": "map(string)",
    "description": "Map of additioanl IAM role policy ARNs to attach to the IAM role",
    "default": "{}",
    "required": false
  },
  {
    "name": "execution_iam_role_permissions_boundary",
    "type": "string",
    "description": "ARN of the policy that is used to set the permissions boundary for the IAM role",
    "default": "null",
    "required": false
  },
  {
    "name": "create_infrastructure_iam_role",
    "type": "bool",
    "description": "Determines whether the ECS infrastructure IAM role should be created",
    "default": "true",
    "required": false
  },
  {
    "name": "task_iam_role_max_session_duration",
    "type": "number",
    "description": "Maximum session duration (in seconds) for ECS task role. Default is 3600.",
    "default": "null",
    "required": false
  },
  {
    "name": "security_group_use_name_prefix",
    "type": "bool",
    "description": "Determines whether the security group name (`security_group_name`) is used as a prefix",
    "default": "true",
    "required": false
  },
  {
    "name": "create_task_iam_role",
    "type": "bool",
    "description": "Determines whether the ECS task IAM role should be created",
    "default": "true",
    "required": false
  },
  {
    "name": "create_cloudwatch_log_group",
    "type": "bool",
    "description": "Determines whether a log group is created by this module for the cluster logs. If not, AWS will automatically create one if logging is enabled",
    "default": "true",
    "required": false
  },
  {
    "name": "create_security_group",
    "type": "bool",
    "description": "Determines if a security group is created",
    "default": "true",
    "required": false
  },
  {
    "name": "execution_secret_arns",
    "type": "list(string)",
    "description": "List of SecretsManager secret ARNs the task execution role will be permitted to get/read",
    "default": "[]",
    "required": false
  },
  {
    "name": "cloudwatch_log_group_retention_in_days",
    "type": "number",
    "description": "Number of days to retain log events",
    "default": "14",
    "required": false
  },
  {
    "name": "tags",
    "type": "map(string)",
    "description": "A map of tags to add to all resources",
    "default": "{}",
    "required": false
  },
  {
    "name": "health_check_path",
    "type": "string",
    "description": "Path for health check requests. Defaults to `/ping`",
    "default": "null",
    "required": false
  },
  {
    "name": "vpc_id",
    "type": "string",
    "description": "The ID of the VPC where the security group will be created",
    "default": "null",
    "required": false
  },
  {
    "name": "execution_iam_role_arn",
    "type": "string",
    "description": "Existing IAM role ARN",
    "default": "null",
    "required": false
  },
  {
    "name": "network_configuration",
    "type": "object({\n    security_groups = optional(list(string), [])\n    subnets         = optional(list(string))\n  })",
    "description": "The network configuration for task in this service revision",
    "default": "null",
    "required": false
  },
  {
    "name": "security_group_egress_rules",
    "type": "map(object({\n    name = optional(string)\n\n    cidr_ipv4                    = optional(string)\n    cidr_ipv6                    = optional(string)\n    description                  = optional(string)\n    from_port                    = optional(string)\n    ip_protocol                  = optional(string, \"tcp\")\n    prefix_list_id               = optional(string)\n    referenced_security_group_id = optional(string)\n    tags                         = optional(map(string), {})\n    to_port                      = optional(string)\n  }))",
    "description": "Security group egress rules to add to the security group created",
    "default": "{}",
    "required": false
  },
  {
    "name": "execution_iam_role_use_name_prefix",
    "type": "bool",
    "description": "Determines whether the IAM role name (`execution_iam_role_name`) is used as a prefix",
    "default": "true",
    "required": false
  },
  {
    "name": "execution_iam_role_max_session_duration",
    "type": "number",
    "description": "Maximum session duration (in seconds) for ECS task execution role. Default is 3600.",
    "default": "null",
    "required": false
  },
  {
    "name": "execution_iam_policy_path",
    "type": "string",
    "description": "Path for the iam role",
    "default": "null",
    "required": false
  },
  {
    "name": "infrastructure_iam_role_arn",
    "type": "string",
    "description": "Existing IAM role ARN",
    "default": "null",
    "required": false
  },
  {
    "name": "task_iam_role_tags",
    "type": "map(string)",
    "description": "A map of additional tags to add to the IAM role created",
    "default": "{}",
    "required": false
  },
  {
    "name": "cloudwatch_log_group_class",
    "type": "string",
    "description": "Specified the log class of the log group. Possible values are: `STANDARD` or `INFREQUENT_ACCESS`",
    "default": "null",
    "required": false
  },
  {
    "name": "cluster",
    "type": "string",
    "description": "Name or ARN of the ECS cluster. Defaults to `default`",
    "default": "null",
    "required": false
  },
  {
    "name": "name",
    "type": "string",
    "description": "Name of the service. If not specified, a name will be generated. Changing this forces a new resource to be created",
    "default": "\"\"",
    "required": false
  },
  {
    "name": "execution_iam_role_description",
    "type": "string",
    "description": "Description of the role",
    "default": "null",
    "required": false
  },
  {
    "name": "infrastructure_iam_role_name",
    "type": "string",
    "description": "Name to use on IAM role created",
    "default": "null",
    "required": false
  },
  {
    "name": "infrastructure_iam_role_path",
    "type": "string",
    "description": "IAM role path",
    "default": "null",
    "required": false
  },
  {
    "name": "infrastructure_iam_role_description",
    "type": "string",
    "description": "Description of the role",
    "default": "null",
    "required": false
  },
  {
    "name": "infrastructure_iam_role_permissions_boundary",
    "type": "string",
    "description": "ARN of the policy that is used to set the permissions boundary for the IAM role",
    "default": "null",
    "required": false
  },
  {
    "name": "security_group_ingress_rules",
    "type": "map(object({\n    name = optional(string)\n\n    cidr_ipv4                    = optional(string)\n    cidr_ipv6                    = optional(string)\n    description                  = optional(string)\n    from_port                    = optional(string)\n    ip_protocol                  = optional(string, \"tcp\")\n    prefix_list_id               = optional(string)\n    referenced_security_group_id = optional(string)\n    tags                         = optional(map(string), {})\n    to_port                      = optional(string)\n  }))",
    "description": "Security group ingress rules to add to the security group created",
    "default": "{}",
    "required": false
  },
  {
    "name": "security_group_tags",
    "type": "map(string)",
    "description": "A map of additional tags to add to the security group created",
    "default": "{}",
    "required": false
  },
  {
    "name": "task_iam_role_use_name_prefix",
    "type": "bool",
    "description": "Determines whether the IAM role name (`task_iam_role_name`) is used as a prefix",
    "default": "true",
    "required": false
  },
  {
    "name": "task_iam_role_path",
    "type": "string",
    "description": "IAM role path",
    "default": "null",
    "required": false
  },
  {
    "name": "cloudwatch_log_group_tags",
    "type": "map(string)",
    "description": "A map of additional tags to add to the log group created",
    "default": "{}",
    "required": false
  },
  {
    "name": "cpu",
    "type": "string",
    "description": "Number of CPU units used by the task. Valid values are powers of `2` between `256` and `4096`",
    "default": "null",
    "required": false
  },
  {
    "name": "execution_iam_role_policies",
    "type": "map(string)",
    "description": "Map of IAM role policy ARNs to attach to the IAM role",
    "default": "{}",
    "required": false
  }
]
